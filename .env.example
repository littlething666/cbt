# Server-only secret. In Vercel, set this on the project's Environment Variables.
# Do NOT commit a real key.
OPENROUTER_API_KEY=

# Used as the OpenRouter HTTP-Referer header. Defaults to NEXT_PUBLIC_APP_URL when set.
NEXT_PUBLIC_APP_URL=http://localhost:3000
